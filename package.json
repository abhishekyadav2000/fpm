{
  "name": "fpm",
  "private": true,
  "version": "0.0.0",
  "description": "Financial Posture Management - Local-first personal finance with AI",
  "packageManager": "pnpm@8.15.0",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "prettier": "^3.2.0",
    "eslint": "^8.56.0",
    "typescript": "^5.3.3"
  }
}
